#include <stdbool.h>
#include <stdio.h>

int main()
{
    bool flag = false;
    // add more later!
    if (flag)
        printf("The flag is true");
    else
        printf("The flag is false");
    
    return(0);

}